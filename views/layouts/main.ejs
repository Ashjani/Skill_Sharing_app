<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <title>
      <%= typeof title !== 'undefined' ? title : 'Skill Sharing Community' %>
    </title>

    <!-- Favicon (put favicon.ico in /public) -->
    <link rel="icon" href="/favicon.ico" type="image/x-icon" />
    <!-- If you don't want an icon yet, you can silence the browser request with: -->
    <!-- <link rel="icon" href="data:," /> -->

    <!-- Materialize CSS & Icons -->
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/icon?family=Material+Icons"
      rel="stylesheet"
    />

    <!-- App CSS -->
    <link href="/css/main.css" rel="stylesheet" />
    <link href="/css/services.css" rel="stylesheet" />

    <!-- Per-page extra CSS (optional: set res.locals.pageStyles in a view) -->
    <% if (typeof pageStyles !== 'undefined') { %><%= pageStyles %><% } %>
  </head>
  <body>
    <!-- Navbar -->
    <%- include('../partials/navbar'); %>

    <!-- Main content from each view -->
    <main style="min-height: 70vh"><%- body %></main>

    <!-- Footer -->
    <%- include('../partials/footer'); %>

    <!-- Materialize JS -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>

    <!-- App JS -->
    <script src="/js/app.js"></script>

    <!-- Per-page extra scripts (optional: set res.locals.pageScripts in a view) -->
    <% if (typeof pageScripts !== 'undefined') { %><%= pageScripts %><% } %>

    <!-- Global form validations -->
    <script src="/js/validation.js"></script>
  </body>
</html>
